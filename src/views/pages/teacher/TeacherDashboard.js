import DashboardLayout from "../layout/DashboardLayout";

const TeacherDashboard = () => {
  return (
    <DashboardLayout title={"Teacher Dashboard"}>
      <h2 className="text-3xl">Teacher Dashboard</h2>
    </DashboardLayout>
  );
};

export default TeacherDashboard;
